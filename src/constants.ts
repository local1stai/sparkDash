/** Reserved tab id for the cross-Spark overview (not a real Spark). */
export const OVERVIEW_ID = "__overview__";

/** Reserved history-series id for the fleet-level fan RPM (not a Spark id). */
export const FAN_SERIES_ID = "__fan__";